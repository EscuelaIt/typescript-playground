enum Direction {
  Up,
  Down,
  Left,
  Right,
}

type TypeDirection = 'Up' | 'Down' | 'Left' | 'Right'

enum Emoji {
  LEMON = '🍋',
  ORANGE = '🍊',
}

enum Numbers {
  ONE = 1,
  TWO,
}

export {}
